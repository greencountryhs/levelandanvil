# Full-resolution image originals

These files are backups of pre-optimization photos used on the public site.
They are excluded from Vercel deploys via `.vercelignore` so they are not
served as production assets.

Public pages use resized WebP files under `/images/` and `/images/responsive/`.
